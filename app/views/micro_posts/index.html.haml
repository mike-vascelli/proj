%p#notice= notice
%h1 Listing Micro Posts

%table
  %thead
    %tr
      %th User
      %th Text
      %th{:colspan => "3"}
  %tbody
    - @micro_posts.each do |micro_post|
      %tr
        %td= micro_post.user_id
        %td= micro_post.text
        %td= link_to 'Show', micro_post
        %td= link_to 'Edit', edit_micro_post_path(micro_post)
        %td= link_to 'Destroy', micro_post, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Micro post', new_micro_post_path
%br/
- @micro_posts.each do |p|
  %p= p.text
  %p= p.user.name
